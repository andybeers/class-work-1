// eslint-disable-next-line no-unused-vars
import React, { Component, PropTypes } from 'react';

export default class ChatApp extends Component {
  static propTypes = {
  }

  render() {
    return (
      <div className='chat-app'>
        I am a full-height container.
      </div>
    );
  }
}
